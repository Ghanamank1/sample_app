<%= provide(:title, 'All users') %>

<h1> All users </h1>

<div class="pagination">
<%= will_paginate %>
</div>

<ul class="users">
    <% @users.each do |user| %>
        <%= render user %>
    <% end %>
</ul>

<%= will_paginate %>